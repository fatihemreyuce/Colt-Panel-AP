export interface officeRequest{
    name: string;
    address: string;
    phoneNumber: string;
}

export interface officeResponse{
    id: number;
    name: string;
    address: string;
    phoneNumber: string;
}